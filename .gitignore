# Secrets. Never commit these.
.env
.env.*
*.token

# Environment
.venv/
__pycache__/
*.py[cod]

# Data and checkpoints are large and reproducible.
data/
checkpoints/
*.bag
*.pt
*.safetensors

# Third-party, cloned not vendored.
lichess-bot/

# Runtime
logs/
*.pgn
reference/
research/attempts/
research/best.py
runs/
runs-*.log
bot.env
*.env

# Local working notes, deliberately not published (see commit "Drop
# references to removed internal docs"). Kept on disk for future sessions.
CLAUDE.md

# Third-party binaries, downloaded not vendored. 110MB and reproducible:
# see the Stockfish note in CLAUDE.md for the exact release and how to refetch.
tools/
