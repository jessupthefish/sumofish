[Unit]
Description=Check every 5 minutes whether the SumoFish training run has stopped

[Timer]
OnCalendar=*:0/5
# Fire a missed check on wake rather than skipping the window silently.
Persistent=true

[Install]
WantedBy=timers.target
